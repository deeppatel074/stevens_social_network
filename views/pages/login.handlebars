<!DOCTYPE html>
<html lang="en">

<head>
  <title>Login</title>
  <link rel="stylesheet" href="/public/css/style.css">
</head>

<body>
  <div class="login-box">
    <h1>Login</h1>

    {{#if hasErrors}}
    <div class="alert">
      <span class="closebtn" onclick="this.parentElement.style.display='none';">&times;</span>
      <strong>{{errors}}</strong>
    </div>
    {{/if}}
    <form id="login-form" action="/login" method="POST">
      <label>Email</label>
      <input type="email" id="email" name="email" placeholder="Stevens Email" value='{{email}}' required />

      <label>Password</label>
      <input type="password" id="password" name="password" placeholder="Password" value='{{password}}' required />

      <button type="submit">Submit</button>

    </form>
  </div>
  <p class="para-2">
    Not have an account? <a href="/signup/">Sign Up Here</a>
  </p>
</body>

</html>